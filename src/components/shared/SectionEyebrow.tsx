export function SectionEyebrow({ children }: { children: React.ReactNode }) {
  return (
    <span className="font-heading text-[11px] font-semibold uppercase tracking-[0.25em] text-kult-red">
      {children}
    </span>
  );
}
